---
layout: layout.njk
title: 首页
---

<section class="hero">
    <h2>发现美味蛋糕</h2>
    <p>精选新鲜原料，手工制作，为您带来甜蜜体验</p>
</section>

<section class="gallery">
    {% set cakes = collections.cakes | sort(false, false, "data.order") %}
    {% for cake in cakes %}
        {% if cake.data.published %}
        <article class="cake-card">
            <div class="cake-image">
                {% if cake.data.image %}
                <img src="{{ cake.data.image | url }}" alt="{{ cake.data.title }}">
                {% else %}
                <div class="image-placeholder">🍰</div>
                {% endif %}
            </div>
            
            <div class="cake-info">
                <h3>{{ cake.data.title }}</h3>
                <p class="description">{{ cake.data.description }}</p>
                
                <div class="cake-meta">
                    {% if cake.data.flavor %}
                    <span class="flavor">{{ cake.data.flavor }}</span>
                    {% endif %}
                    {% if cake.data.size %}
                    <span class="size">{{ cake.data.size }}</span>
                    {% endif %}
                </div>
                
                <div class="price">{{ cake.data.price }}</div>
                
                <button class="contact-btn" onclick="copyWechat('{{ site.wechat }}')">
                    📞 联系店家咨询
                </button>
            </div>
        </article>
        {% endif %}
    {% else %}
        <div class="empty-state">
            <h3>暂无蛋糕展示</h3>
            
            <!-- 添加管理面板 -->
            <section class="admin-panel">
                <h2>添加新蛋糕</h2>
                <form class="cake-form" name="add-cake" netlify>
                    <div class="form-group">
                        <label>蛋糕名称：</label>
                        <input type="text" name="title" required>
                    </div>
                    
                    <div class="form-group">
                        <label>描述：</label>
                        <textarea name="description" rows="3" required></textarea>
                    </div>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label>价格：</label>
                            <input type="text" name="price" placeholder="例如：￥168" required>
                        </div>
                        
                        <div class="form-group">
                            <label>口味：</label>
                            <input type="text" name="flavor" placeholder="例如：芒果慕斯">
                        </div>
                        
                        <div class="form-group">
                            <label>尺寸：</label>
                            <input type="text" name="size" placeholder="例如：6英寸">
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label>图片URL：</label>
                        <input type="url" name="image" placeholder="https://example.com/cake.jpg">
                        <small>提示：您可以先将图片发到微信群，然后复制图片链接</small>
                    </div>
                    
                    <button type="submit" class="submit-btn">提交蛋糕信息</button>
                </form>
            </section>
        </div>
    {% endfor %}
</section>
